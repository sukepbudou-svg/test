# 🐻㊙裏熊予想ツール — プロジェクトメモリ

このファイルは競艇（ボートレース）予想ツールの開発コンテキストを新しいセッションに引き継ぐためのもの。
コードを触る前に必ず全部読むこと。

## プロジェクト概要

- **目的**: ユーザーが公式サイトのデータを貼り付けて三連単の買い目予想を出し、成績を記録・分析する個人用Webツール。最終目標は「最強の予想ツール」＝回収率100%超の安定運用
- **構成**: Flask (Python) + SQLite (`boatrace-web/history.db`) + テンプレート2枚（`index.html`=予想画面, `history.html`=成績記録）
- **起動**: ユーザーはWindows。`cd C:\Users\user\test && git pull && python boatrace-web/app.py`（ポート5001）
- **ユーザーへの説明は日本語**。技術用語はかみ砕く。ユーザーは開発者ではないが鋭い指摘をする

## 現在の予想ロジック（MODEL_VERSION: v9, 2026-07-07〜）

**v9（グループB重み1.5倍・採用済み 2026-07-07）**: v7をベースに、選手勝率・級別・モーター・会場×コース別1着率（グループB＝地力・機材）のスコア係数を1.5倍にする補正。単一要素モデル比較（グループA「当日の調子」vs グループB「地力・機材」）で「本命的中率はグループB単独の方が複合モデル(v7)より高かった」という結果を受けて追加。
- 根拠: 335レースのバックテストで 本命12.5%→14.3%（+1.8pt）、対抗14%→15.5%（+1.5pt）、5点回収率88.2%→98.9%（+10.7pt）。イン逃げ率65%以上96レースに絞っても同じ方向で改善（本命+3.1pt、対抗+3.2pt、回収率81.7%→90.7%）と一貫性を確認
- `app.py`: `USE_V9_GROUP_B_BOOST_LIVE = True`。`predict()`の`v9_group_b_boost`引数で、class_bonus・player_bonus・motor_bonus・course_rates項に一律×1.5。バックテストのvariantドロップダウンで「v9（現在の実戦・グループB重み1.5倍）」として常時比較可能
- 同時に用意した「v9+超展開データ」（`v9_cho_tenkai`、候補の並び替え基準をcombinedからnakaana_scoreに変更）は、超展開データの保存開始（2026-07-05）から日が浅くカバー率が低いため、v9と完全に同じ結果（差なし）だった。**まだ採用判断できる材料がないため実戦には反映していない**。データが増えたら再検証すること
- **注意**: これは同じ過去データを見ながら係数を決めた事後検証なので、真の実力は今後の新規レースで確認する必要がある。次の節目（100〜150件など）で効果が続いているか再チェックすること

## v7（2026-07-05〜2026-07-07・v9採用前まで実戦だったモデル）

**v7（本命3着改善）**: 本命2点目の3着を「逃がし3着率」（kimariのsimデータ）最上位の艇に差し替え。本命1点目は変更なし。対抗のロジックは無変更だが、本命2点目の3着が変わることでused_combos（重複除外）が変わり、対抗の並びも間接的に影響を受ける。
- 根拠: 得意会場×65%以上71レースのバックテストで 本命16.9%→21.1%（+4.2pt）、5点回収率106%→121.7%（+15.7pt）
- 同時に検証した「2着バグ修正」「v5（決まり手展開補正）」は**3回の検証（87/18/71レース）すべてで完全に無効果**と確定したため不採用。`USE_V5_LIVE`は恒久的にFalse、v7の2着修正要素も使わない
- `app.py`: `USE_V7_THIRD_LIVE = True`（v9でも維持）。バックテストのvariantドロップダウンで「v7（旧・実戦相当）」として比較可能
- **教訓**: 18レースでは「本命-1・対抗+1のラベル移動」に見えて不採用にしたが、71レースまで増えたら明確なプラスに転じた。**小サンプルでの「効果なし」判定は覆ることがある**ため、データが増えたら過去に見送ったロジックも再検証する価値がある

**v6ハイブリッド対抗（採用済み 2026-07-04）**: イン逃げ率65%以上のレースは対抗3点を1位艇1着固定（旧v3方式）、65%未満は3シナリオ（逆転/ST展開/次点）。根拠: 得意会場×65%以上18レースのバックテストで対抗11.1%→27.8%。「勝負レース＝堅い前提で選んだレースで逆転を買うのは矛盾」という設計判断。

## v4までのベースロジック（2026-07-03）

`app.py` の `predict()`:

- **スコア計算**: 展示タイム・平均ST・チルト・F持ち・級別（A1+0.8/A2+0.4/B1 0/B2-0.5、展示ST悪いほど級差拡大）・周回/直線/まわり足（当日×0.6+平均×0.4、**小さいほど速い=高評価**。v4でここの符号バグを修正した）・選手勝率・モーター・会場×コース別1着率・風・展開補正（展示STのコース間比較）
- **買い目構成**: 本命2点＋対抗3点（中穴は廃止済み）＋フロント側で万舟2点（オッズ100〜200倍から）
  - 本命2点: スコア1位艇が1着の上位2コンボ
  - 対抗1点目: スコア2位艇が1着（逆転シナリオ）
  - 対抗2点目: 1位艇1着＋展示ST最速艇が2着（展開シナリオ）
  - 対抗3点目: used_combosで重複除外した次点
- **確率化**: candidatesのスコアをsoftmaxで確率化（`prob`）→ EV表示に使用
- **ロジックを変更したら `MODEL_VERSION` を必ず上げる**（v5, v6...）。コメントに日付と変更内容を書く

## 買い方の戦略（ユーザーと合意済み）

- **勝負レースの条件**: イン逃げ率が会場別の参戦ライン以上（デフォルト65%、後述の会場別最適化あり） ＋ 本命1点目オッズ7倍以上 ＋ 得意会場（その会場×逃げ率帯で回収率80%以上）
- **会場別・逃げ率の参戦ライン個別最適化（2026-07-07実装）**: `/venue_nige_thresholds`で得意会場ごとに55〜80%の各ラインの実力回収率を比較した結果、**大村・若松は65%より60%の方がレース数も多く回収率も良かった**ため、両会場だけ参戦ラインを60%に引き下げ。徳山・福岡・桐生は65%のままで問題なさそうだったため維持。`app.py`の`VENUE_NIGE_THRESHOLD = {'大村': 60, '若松': 60}`（デフォルトは`DEFAULT_NIGE_THRESHOLD = 65`）で管理し、`_nige_threshold(venue)`関数で参照。`_judge_tier`・`/stats_summary`のnige_min計算・`/weekly_report`の✅勝負レース集計・index.htmlの勝負レースバナー（`nigeThresholdFor(venue)`）に反映済み。curlでの動作確認済み（大村は67%入力でband=60、福岡は67%入力でband=65になることを確認）。**まだ実データでの新規レースでの効果検証はしていない**ため、次の節目で60%ラインが本当に良かったか再確認すること
- **会場グループ（2026-07-04ユーザー設定、2026-07-05一部更新。index.html/history.html/app.pyのTOKUI_VENUES/KENSHO_VENUESと同期）**:
  - 得意会場（✅勝負対象）: 大村・福岡・桐生・徳山・**若松**（2026-07-07追加。検証中会場からの正式昇格。分析レポートで11レース・実力回収率105.8%と「10レース以上+100%以上」の昇格基準を満たしたため）
  - 検証中会場（🔍データ蓄積で判定）: 宮島・常滑・三国・尼崎・蒲郡・多摩川・びわこ・**津・丸亀**（2026-07-05追加。元は対象外だったが「傾向として苦手なだけで絶対ダメではない」ため検証中に格上げ）
  - 対象外（⚠️見送り・自動昇格は高いハードルのみ）: 戸田・江戸川・平和島・鳴門・下関・唐津・**児島・住之江・浜名湖・芦屋**（2026-07-07降格。分析レポートで「⬇降格候補」表示: 児島22レース・実力回収率71.2%／住之江15レース・62.5%／浜名湖17レース・46%／芦屋14レース・44.2%。手動でKENSHO_VENUESから除外し対象外へ移動。対象外会場からの復帰は下記の高いハードルの自動昇格のみ）
  - 検証中会場は成績が貯まったら得意会場へ昇格/対象外へ降格の判断をする
  - **⚠️検証中→対象外の自動降格は未実装（2026-07-07ユーザー確認）**: 得意会場は実力回収率80%未満（10レース以上）で自動的に⚠️見送りに格下げされるが、検証中会場には対応する自動降格ロジックがなく、成績が悪くても🔍検証中のまま表示され続ける。今回の児島等4会場は手動でリストを書き換えて対応した。**自動化は今後の実装予定**（閾値設計に注意: 意図しない会場まで降格させるリスクがあるため）
  - **対象外会場の自動昇格（2026-07-05実装）**: 経験則で苦手と判断した会場なので、検証中会場（10レース+実力回収率100%で昇格）より高いハードル（**20レース以上+実力回収率120%以上**）を課す。2026-07-06にサーバー側`_judge_tier`へ移植済みのため、成績記録側でも同じ判定が反映される
- 実測の裏付け: 2026-07-04当日データで 全体78% → 逃げ率+オッズ条件76% → +会場絞り**154%**（7レース、要追加検証）
- 会場絞り込みの効果は実証済み: 全体回収率67% → 絞ると108%
- 判定はindex.htmlの「勝負レース判定バナー」が自動化済み。会場判定は「今回の逃げ率を5%刻みで切り下げた値〜100%」の帯で過去成績を見る（**下の帯を混ぜてはいけない**、ユーザーの強い意向）。**ただし65%以上は一律65%以上でまとめる**（2026-07-06修正。65%以上を5%刻みで細分化すると例えば逃げ率80%のレースが「80%以上」だけの少数データで判定されてしまうため。65%が唯一の閾値として運用されている以上、65〜100%は常に同じ帯として扱うのが正しい）。`/stats_summary`のnige_min計算・`_judge_tier`のband_min計算・index.htmlのbandMin計算の3箇所に反映済み
- 万舟2点は検証継続中（65%以上絞りで回収期待値173%だったがサンプル4回。50レース貯めて判断する約束）
- 本命が10倍以下の堅いレースは回収困難。見送りか本命のみ推奨
- **ユーザーの買い方パターン**: 本命の倍率が低いとき（7倍未満等）は本命2点だけ購入する場合がある。成績分析やシミュレーションの際はこのパターンを考慮すること。odds_all蓄積後に「5点フル/本命2点のみ/1点目抜き」等の買い方別回収率シミュレーションで検証予定

## 実装済みの主要機能

1. **データ貼り付けパース**: 一括貼り付け／モーター＋選手情報（級別含む）／今節展示情報＋展示情報の統合パース／オッズ
2. **F持ちは手動のみ**（自動判定は廃止済み、ユーザーの意向）
3. **会場・レース変更時に全クリア**（選手の級バッジも消える）
4. **成績記録**: 予想保存（オッズ・入力データ・モデルバージョン込み）→結果入力（公式サイト自動取得あり）→集計
5. **成績分析**: サーバー側集計 `/history_stats`（デバウンス200ms、行はDOM show/hide）。種別別的中率・平均配当・回収期待値、**外れ方内訳**（本命・対抗それぞれ: 的中/3着だけズレ/2・3着ズレ/1着から外れ）、会場別、風向き別、日別、収支推移
6. **成績記録の初期表示は「当日」**（JST）。当日/7日/30日/全期間ボタン。結果入力の購入額デフォルトは500円（5点×100円）
7. **バックテスト** `/backtest`: input_data付きレースを現行ロジック（v5+v6）で再予想して保存時予想と比較。**成績記録の絞り込み（期間・会場・逃げ率）に連動**。旧v3対抗シミュ行あり。odds_all付きレースでは本命対抗5点の回収率比較も表示。**2026-07-03以降の保存分のみ対象**
8. **EV表示**: 各買い目に EV=モデル確率×オッズ。100%以上緑。確率は未キャリブレーション（粗い）と明言済み。**EVでの買い目選別はキャリブレーション完了までしない方針**
9. **予想は倍率帯で再分類しない**: サーバーのtype割り当てをそのまま表示（classifyByScore、TYPE_LIMITS制限なし）。以前は倍率帯で上書きしていて対抗の逆転シナリオが表示されないバグがあった
10. **勝負レース判定（3段階・2026-07-04）**: ✅勝負（得意会場+逃げ率65%+本命7倍以上）/🔍検証中会場/⚠️見送り。会場データ判定は**実力回収率**（本命対抗5点ベース・万舟の的中を除外した`recovery_base`）を使用。得意会場でも実力回収率80%未満（10レース以上）なら⚠️に格下げ、🔍でも100%以上なら「✅勝負レース（データ昇格）」に自動昇格。成績記録にも判定バッジ（R列横）と判定別成績パネルあり
    - **✅修正済み（2026-07-06）**: 従来はこの不整合があった（予想画面バナーは実力回収率で会場格下げ・昇格するが、成績記録の`judgeRaceTier`は逃げ率+オッズの2条件のみで判定しズレていた）。`/get_records`がサーバー側で各レコードの`tier`（'shobu'/'kensho'/'miokuri'）を予想画面バナーと同じロジック（新設の`_judge_tier`関数、会場の実力回収率による格下げ・昇格込み）で計算して返すように変更し、history.htmlの`judgeRaceTier(r)`は`r.tier`を返すだけの薄いラッパーになった。判定ロジックが一箇所（app.py）に統一されたので、今後は両画面で必ず一致する。実装: `is_henkan_combo`/`_calc_recovery_base`/`_judge_tier`をapp.py内のモジュール共通関数として切り出し、`/history_stats`もこれらを再利用するようリファクタ。curlでの動作確認済み（得意会場×低回収率→miokuriに格下げ、検証中会場×高回収率→shobuに格上げ、両方とも意図通り）
11. **返還対応（F等）**: 結果入力に返還艇欄。返還コンボ×100円を購入額から自動減算、集計では返還コンボを「無効」扱い（外れにしない）。全点返還は購入0円=±0円(返還)表示
12. **保存データ**: predictionsに各コンボのオッズ、input_dataに全入力+決まり手全データ（kimari_full）+**全120通りオッズ（odds_all）**、model_version、henkan。odds_allにより後から任意の買い方のシミュレーションが可能
13. **女子戦フラグ（2026-07-06実装）**: 予想画面ヘッダーに「女子戦」チェックボックス。DBの`is_womens`列に保存。「女子戦は荒れやすいのでは」という仮説を検証するための土台。成績記録に性別フィルター（すべて/女子戦のみ/女子戦除く）、週次レポートに女子戦/女子戦以外の成績サマリー。バックテスト・買い方シミュレーター等の共通フィルタ（`_filtered_result_rows`）にも対応済みなので、女子戦だけの買い方比較も可能。**まだ仮説段階、データが貯まってから検証すること**
14. **2連単換算チェック（2026-07-06実装、2026-07-07に裏熊対応追加）**: オッズ貼り付けデータに実は「2単」ラベルで2連単オッズ（全30通り）が含まれていた。parseOdds()は元から`tan2`配列に読み取っていたが未使用だったため、`getBiOdds(from,to)`で取得・保存に対応。**ユーザー提案の検証機能**：「3連単は外れても1-2着は合っている」ケースを本命・対抗・🐻裏熊それぞれ集計し、2連単換算なら的中率・回収率がどうなるかを見る（`/nirentan_check`、成績記録の「🎯2連単換算チェック」カード）。的中率は全レースで即集計可能、回収率は`bi_odds`保存済みレース（2026-07-06以降の保存分）のみ。もし2連単の回収率が3連単より明確に高ければ、舟券の種類を変える判断材料になる。**2026-07-07: 本命系が堅いレースより荒れ気味のレースで2連単勝負した方がいいのでは、というユーザー仮説の一環で裏熊も対象に追加**（stats辞書に'裏熊'キーを追加、TYPE_BY_INDEXフォールバックに頼らず`p['type']=='裏熊'`で直接判定）
15. **🎯中穴予想（2026-07-07実装・検証段階）**: 20〜99倍の中間帯を狙う新モデル。**裏熊と同じく「💰 中穴予想」専用ボタンで呼び出す独立モード**（当初は通常予想ボタンに自動で混ぜていたが、「通常予想は従来と同じ条件でデータを取り続けたい」というユーザー要望で独立ボタンに変更）。`runPredict(mode)`のmodeが'normal'/'arekote'/'nakaana'の3種類になり、renderResult内でmode別に表示・保存内容を分岐。中穴モードでは本命/対抗/万舟/裏熊は一切表示・保存されず、中穴6点のみが対象。ロジックは`data.candidates`（スコア上位60件）を20〜99倍の範囲でフィルタし、**20〜40倍／40〜70倍／70〜99倍の3帯に分けて各帯からスコア上位2点ずつ＝計6点**を選ぶ（ユーザー要望「可能性が高い順に選びつつ倍率帯は偏らせない」に対応。スコアはモデル独自の強さ評価であり、オッズ人気とは無関係）。保存時は`type:'中穴'`のみのレコードとなり、既存の型別集計インフラ（`/history_stats`のpattern、`/payout_stats`、weekly_report等）にそのまま乗る（旧モデルがv4で使っていた'中穴'枠が空いていたため再利用）。**裏熊と同様、まずは記録のみで実弾なし。回収率が確認できてから実弾に移行する方針**（ユーザー確認済み）。結果入力の購入額デフォルトは`保存点数×100円`に統一済み（従来は通常予想=固定500円だったため、6点の中穴レコードだとズレる）
    - **超展開データ活用（2026-07-07追加）**: 「中穴予想は始まったばかりなので、渡しているデータで使えそうなものは全部使ってほしい」というユーザー要望で、超展開データ（`extra_stats.cho_tenkai`）も中穴のスコアに反映。app.pyに`_nakaana_attack_bonus(cho_tenkai)`を追加し、2〜6号艇それぞれの差し/まくり/まくり差し成功率（試行回数3回以上のみ信頼）の最大値を「攻めボーナス」として算出。`predict()`内で候補コンボごとに`nakaana_score = combined + 2着艇のボーナス×0.05 + 3着艇のボーナス×0.03`を計算し、`combined`/`prob`とは別の新フィールドとして返す（本命/対抗/裏熊の選定ロジックは一切変更なし、影響しない）。index.htmlの中穴帯選定は`nakaana_score`があればそれを優先して並べ替える。超展開データが無い場合は`nakaana_score`=`combined`にフォールバックするので今まで通り動く。これにより「超展開データが中穴予想に効くか」を最初から検証できる（本命/対抗にはまだ使わない、中穴限定の実験場という位置づけ）

## 🐻裏熊予想（2026-07-04実装・v3 2026-07-05更新・検証段階）

高配当狙いの独立システム。通常の本命対抗とは**別の財布・別の成績**で運用する。**裏熊は一切実弾を張っていないため、通常予想と違い凍結せず検証中でもロジック更新してよい**（ユーザー方針・2026-07-05）。

- **出動条件（AND、バナーに充足数表示）**: (1)イン逃げ率40%未満 (2)1号艇に弱点2つ以上（スコア3位以下/B級/展示ST5番手以下/差され捲られ率30%超/**抵抗1回以下**）(3)仕留める主役がいる（3〜5コース・まくり系15%以上・STが1号艇より速い・スコア2位以内） (4)オッズ妙味（20倍未満は足切り、全滅なら見送り表示で買い目なし）
- **買い目**: 主役-相手2艇-全 のフォーメーション8点（足切り後は減る）。型は穴党プロの定石「1着シナリオ固定・3着は面で拾う」。狙いは30〜150倍
- **裏熊ボタンはいつでも押せる**。条件不足時は「参考予想（2/4）」表示で保存は可能
- **出動ライン（2026-07-07実装）**: `/ura_backtest`で出動条件クリア数別の回収率を検証した結果、**3/4以上クリアの回収率だけが唯一プラス（110%、59レース）**で、1/4・2/4は明確なマイナス、4/4はまだ12レースしかなくデータ不足のため保留。予想画面のバナーに「🎯出動ライン到達（3/4以上）」を緑色で表示するようにした（オッズ込みの4条件が揃っている場合のみ表示、3条件のみの時は非表示）。成績記録のR列横バッジも3/4以上は緑色で強調。裏熊全体の回収率はまだ100%未満（66.3%）なので、3/4以上に絞った上でさらに実績を積んでから実弾化を検討すること
- **成績記録でも出動条件クリア数を表示（2026-07-07実装）**: 結果入力時に「イン逃げ率が○%だった時、何/4クリアだったか」が分からないという課題があったため、`/get_records`が裏熊レコードごとに保存済み`input_data`を使って`predict(force_arekote=True, v9_group_b_boost=USE_V9_GROUP_B_BOOST_LIVE)`を再実行し、`_ura_clear_label()`（`/ura_backtest`と共通化）でn/4形式を再計算して`ura_clear`フィールドとして返す。history.htmlはR列の右側（✅勝負等のバッジと同じ位置）に「🐻n/4」を追加表示。行を増やさずに確認できる。curlでの動作確認済み
- **成績は完全分離**: 予想種別フィルター（すべて/通常予想/🐻裏熊）が成績記録の期間ボタン横にある。/history_statsのkindパラメータ。裏熊判定はpredictionsに裏熊タイプを含むか
- 結果入力の購入額デフォルト: 裏熊レコードは点数×100円（通常は500円）
- **検証方針**: 最初の50レースは記録のみ（買わない）。回収率100%超を確認してから実弾
- **v3（2026-07-05）**: 超展開データ（`extra_stats.cho_tenkai`）を主役判定・1号艇弱点・相手選定に活用。試行回数3回未満は信頼度不足としてkimari_fullにフォールバック。実装: `predict_arekote_v3()`（v2は`predict_arekote_v2()`のまま保持、未使用は旧`predict_arekote()`）
- **🐻裏熊バックテスト** `/ura_backtest`（バックテストカード内のボタン）: 裏熊レコードを現行ロジックで再予想し、保存時と比較+出動条件クリア数別（3/3 vs 1〜2/3）の成績を表示。**出動ライン（逃げ率40%等）の妥当性はここで検証する**

## 重要な履歴・教訓

- **2026-07-03にタイム系スコアの符号バグを修正**（周回/直線/まわり足が「遅いほど高評価」になっていた）。それ以前の成績データは全部バグあり予想の成績。新旧を混ぜて判断しない
- **対抗の新ロジック（3シナリオ）も2026-07-03稼働開始**。旧対抗（全部1位艇1着固定）の成績: 65%絞りで15.1%。新ロジックの評価は50レース貯まってから
- 外れ方内訳の知見（65%絞り・旧データ）: 本命の1着艇は72.7%のレースで正解。最大の改善余地は「3着だけズレ」18.9%と「2・3着ズレ」31.5%＝**2着3着の精度が次の課題**
- ユーザーは「モデル凍結期間」に同意済み: データが貯まるまでロジックをいじらない
- 検証で数字を示すときは的中率の分母に注意: **レースベース**（1レース=1カウント）で統一済み。点数ベースと混ぜない
- **イン逃げ率の閾値は統一65%で運用**（2026-07-04合意）。会場別の閾値（60%等）はデータ不足での過剰適合を避けるため保留。得意会場の「60〜65%帯」が30件超えたら会場別ラインを検討する
- **買う規律**: 買うのは✅勝負レースのみ。🔍と⚠️は記録のみ。2026-07-04実測: 全体78%に対し✅相当+会場絞りは154%
- 万舟の高額的中が会場の回収率を歪める問題は`recovery_base`（実力回収率）で対処済み（ユーザーの指摘による）

## 🔔 ユーザーとの約束（次セッションで必ずリマインドすること）

- **v9（グループB重み1.5倍）は採用済み**（2026-07-07）。経緯:
  - 単一要素モデル比較（グループA「当日の調子」vs グループB「地力・機材」）で、335レース検証の結果グループB単独の本命的中率(14%)が複合モデル(12.5%)を上回った
  - v7にグループBの係数×1.5を足したv9候補を作り335レースで再検証: 本命+1.8pt・対抗+1.5pt・回収率+10.7pt（88.2%→98.9%）。65%以上96レースに絞っても同じ方向で改善（本命+3.1pt・対抗+3.2pt・回収率+9.0pt）と一貫性を確認したため採用
  - `USE_V9_GROUP_B_BOOST_LIVE = True`で実戦採用（v6/v7の設定は維持したまま追加）
  - 同時に用意した「v9+超展開データ」は保存データのカバー率がまだ低くv9と差が出なかったため不採用（データが増えたら再検証）
  - **注意**: 同じ過去データを見て決めた係数なので、これからの新規レースで本当に効果が続くか継続確認が必要。次の節目（100〜150件など）でもう一度確認すること

- **v7（本命3着改善）は採用済みだった**（2026-07-05〜2026-07-07、v9採用に伴い置き換え）。経緯:
  - 初回18レース分割検証で「3着差し替えのみ→本命-1/対抗+1、ラベル移動で効果なし」と一度は不採用判定
  - **71レースまで貯まって再検証したところ、本命+4.2pt・5点回収率+15.7pt（106%→121.7%）と明確に改善**、v5・2着バグ修正は3回の検証すべてで完全無効果と確定
  - `USE_V7_THIRD_LIVE = True`は現在も維持（v9はv7に追加する形の変更のため）。`USE_V5_LIVE`は恒久的にFalseで固定
  - **教訓（重要）**: 小サンプル（18件）での「効果なし」判定は絶対ではない。**データが十分増えたら、過去に見送ったロジックも再検証する価値がある**。今後も「一度ボツにしたから終わり」にせず、100〜150件などの節目で再検証を検討すること

- **history.dbのバックアップリマインド**: 成績データ（history.db）はgit管理外でPC故障時に失われる。**約50レース貯まるごと（または週1回）にバックアップを取るようユーザーにリマインドすること**（2026-07-03に合意）
  - 方法: `boatrace-web/history.db` をUSBメモリやGoogleドライブ等にファイルコピーするだけ
  - 特にv5切り替えなど大きな節目の直前は必ず声をかける
  - レース件数確認: `SELECT COUNT(*) FROM records`

## 分析基盤3点セット（2026-07-04実装・データ蓄積で真価を発揮）

成績記録ページに実装済み。すべて絞り込み（期間・会場・逃げ率）連動。odds_all付きレースが対象なので**データが貯まるほど正確になる**。

1. **📋 分析レポート** `/weekly_report`: 全体/通常/✅勝負/🐻裏熊の成績サマリー、検証中会場の昇格・降格候補（10レース以上+実力回収率100%↑=昇格候補）、v5再評価の進捗（150件でリマインド）
2. **🧮 買い方シミュレーター** `/strategy_sim`: 5点フル/本命2点のみ/1点目抜き/対抗のみ/7点/スマート（本命7倍未満は2点）+ 裏熊全点/30〜150倍のみ を一括比較。👑が最良。**「この買い方どうかな」は全部ここで検証してから実弾**
   - **2026-07-05追加**: 本命2点の「最安オッズ」基準でA/B/Cの3ルールを比較（ユーザー提案）: A=どちらかが7倍未満なら見送り／B=安い方の本命1点だけ除外し4点／C=どちらかが7倍未満なら対抗切って本命2点のみ。既存smartは本命1点目だけ見る判定なので別物。データが貯まったらA/B/C/smartのどれが一番得か答えが出る
   - **初回43レース検証（2026-07-05）**: 対抗3点のみ133.7%＞本命1点目抜き4点120.1%＞B114.6%＞7点113.1%＞5点フル106%＞smart96.9%＞A(27件)93.6%＞C79.1%＞本命2点のみ64.5%。**「本命を削る」方向（A/C/本命2点のみ）は軒並み悪化、「本命1点目（一番安いコンボ）だけ削る」方向（1点目抜き/B）が好成績**。理由: 本命1点目は最も的中しやすいが最も配当が低く、削ると残り4点の配当効率が上がる。Aは見送りすぎて機会損失（27件中16件を機会損失扱いで除外）。**まだ43レースなので鵜呑み厳禁、70〜100レースで再検証すること**
   - **得意会場のみ36レース再検証（2026-07-07、v9採用後）**: 👑対抗3点のみ123%(+2,479円)＞スマート108.5%＞5点フル102.1%(+379円、実際の買い方)＞C100.5%≈B100.3%＞A98.4%＞本命1点目抜き4点97.6%＞7点72.9%＞本命2点のみ70.8%。**「対抗3点のみ」が2回連続で好成績を継続**しており有力候補。ただし前回トップだった「本命1点目抜き4点」は今回97.6%で5点フルより悪化しており**逆転**、この施策単体の再現性はまだ不安定。5点フル（現在の実際の買い方）は102.1%とギリギリプラスだが大きくは勝てていない水準
3. **🎓 EVキャリブレーション** `/ev_calibration`: EV帯別にモデル確率vs実測的中率と回収率を検証。「EV100%以上の帯が実際に回収100%超」ならEV信用OK→「緑だけ買う」運用に移行できる。100レース以上で判断
   - **初回415レース×候補60点検証（2026-07-07）**: EV70%未満はモデル確率1.39%に対し実測的中率2.78%（モデルが控えめ）、EV130%以上はモデル確率1.9%に対し実測0.65%（モデルが過大評価）で回収率も75%と全帯で最低。**「EVが高いほど良い」という直感は今のモデルでは通用しない**ことが判明。100〜130%帯だけ回収率108.7%とやや信頼できるが107件程度でまだ判断は早い。**引き続きEVでの買い目選別はしない方針を維持**
4. **📐 会場別・逃げ率ライン比較（2026-07-07実装）**: `/venue_nige_thresholds`。得意会場ごとに逃げ率55/60/65/70/75/80%の各ラインで絞った時の実力回収率・レース数を一覧表示。**「参戦ラインをどこに引くべきか」を会場ごとに個別最適化するための機能**（ユーザーが逃げ率65%一律だと会場ごとの特性を無視しているのでは、と相談したことから作成）。本命+対抗5点ベース・裏熊は除外・全期間対象で`_calc_recovery_base`を再利用。curlでの動作確認済み（テストデータで閾値を上げるとレース数が減り回収率が変動することを確認）
   - **初回検証結果（2026-07-07）**: 大村・若松は65%より60%の方がレース数も多く回収率も良かったため、両会場のみ参戦ラインを60%に引き下げ済み（詳細は下記「買い方の戦略」参照）。徳山・福岡は65%のままで良好、桐生はまだ数字がバラついていて判断つきにくい
5. **🌪 荒れ度スコア検証（2026-07-07実装）**: `/chaos_calibration`。予想画面の「荒れ度」バー（`calc_chaos`関数、風・会場の荒れやすさ・スコア僅差・外枠上位・1号艇F持ちから算出）が実際の結果と相関しているかを検証する。保存済み`input_data`から荒れ度を再計算し、荒れ度帯（低め/やや高め/高い）別に「1号艇以外が1着になった率（荒れ率）」「万舟（オッズ100倍以上）発生率」「平均払戻」を集計。**背景**: 本命対抗の成績が伸び悩み「荒れそうなレースを察知して万舟狙いに転向すべきか」という相談があったが、荒れ予測は本命予測よりデータ的信号が弱く本質的に難しいため、まず今ある荒れ度スコアが実際に機能しているか検証してから判断する方針にした。curlでの動作確認済み（テストデータで高荒れ度帯=荒れ率100%、低荒れ度帯=荒れ率0%を確認）。**まだ実データでの検証は未実施**。もし「高い」帯で荒れ率・万舟発生率が明確に高ければ、荒れ度スコアを万舟予想の判定材料に使う価値がある

## 🤖 自動化プロジェクト（2026-07-07開始・検証段階）

**目的**: ユーザーが毎回手動でデータを貼り付けて予想し、記録し、結果を入力する作業を自動化したい（スクレイピング→自動予想→自動記録→自動結果反映）という要望。ただし**「自動で実際の舟券を購入する」ことは一切しない方針**（ユーザー明言・規約違反リスクを避けるため実装もしない）。またモデルの採否判断（バックテストで効果確認→ユーザーが承認）は自動化せず人の確認を残す方針。

**最大の技術的課題**: 今のv9モデルは「イン逃げ率」（`kimari`のsimデータ）に大きく依存しているが、これは公式サイトではなく**「ボートレース日和」という第三者サイト由来**（スクレイピング禁止をユーザーが確認済み）。展示タイム・ST・チルト・部品交換・選手勝率・級別・モーター成績・オッズは公式サイト（boatrace.jp）に掲載されており問題なく取得できるが、逃げ率相当のデータだけは公式データからの再現が必要。

**方針（ユーザー確定・2026-07-07、真剣に取り組む前提）**: 公式の過去レース結果ページに「決まり手」（逃げ・差し・まくり等）が掲載されているか確認したところ、**掲載されていることを確認済み**（`/debug_html`の`kimari_found`で確認、桐生12Rで「逃げ」を確認）。これにより、過去結果を大量に集めて自前で逃げ率統計を再現できる可能性がある。ボートレース日和を使わない自前計算が本当に使えるか検証してから、次のステップ（自動予想パイプライン構築）に進む。

**実装済み（2026-07-07）**:
1. `/debug_html`に`kimari_found`/`kimari_snippet`を追加。結果ページに決まり手が載っているか確認するツール（成績記録ページに「🔧 決まり手調査ツール」のUIあり、会場・レース番号・日付を選ぶだけ）
2. `_parse_kimari_from_html(html)`: 結果ページのHTMLから決まり手（まくり差し/逃げ/差し/まくり/抜き/恵まれ、の優先順で判定。「まくり」が「まくり差し」の一部分にマッチしてしまわないよう順序に注意）を抽出するヘルパー
3. `/scrape_nige_stats`: 会場・期間（開始日〜終了日）を指定すると、その期間の全レース結果を取得し「1号艇が逃げで1着になった率」を自前計算する。サーバー負荷対策で1回の実行につき最大150リクエスト・0.5秒間隔のスリープを入れている。成績記録ページに「🤖 逃げ率 自前計算検証」のUIあり
4. どちらもこのセッションのサンドボックス環境からはboatrace.jpに到達できない（ネットワーク制限）ため、**ユーザーの実機（Windows）で動作確認が必要**。ユニットテスト（`_parse_kimari_from_html`への文字列入力、エンドポイントのバリデーション・エラーハンドリング）は実施済み

**初回検証結果（2026-07-07・7/7桐生12レース）**: `/scrape_nige_stats`で自前計算した「1号艇が逃げで1着になった率」41.7%（5/12）に対し、ユーザーが貼ったボートレース日和の**レースごとの逃げ率シミュレーション**（1R=0.0%〜12R=62.5%とレースごとに大きく変動）と照合したところ、**日和の予測60%以上のレース6件中5件で実際に逃げが的中、50%未満のレース5件は全部逃げ以外**という綺麗な相関が確認できた。これは重要な発見:
- ボートレース日和の逃げ率は「会場×日付の粗い平均」ではなく、**その日1号艇に入る選手個人の過去実績を反映したレース固有の予測シミュレーション**である（決まり手データの「進入数16〜28」という数字が、選手個人の過去出走回数を示唆している）
- 今の`/scrape_nige_stats`（会場×日付の単純集計）ではこの精度は再現できない。**選手個人単位の統計を作る必要がある**
- ユーザーからは、逃げ率だけでなく「逃がし2着率」「逃がし3着率」「進入数」「逃げ逃し出目確率」（2着艇のコース別内訳込み）という、ボートレース日和が持つより詳細なシミュレーション表も再現したいという要望があった

**追加実装（2026-07-07）**:
5. `/debug_html`に選手登録番号（toban、`toban=数字`パターン）の検出を追加。結果ページのURLから選手個人を特定できるかを確認するため
6. `/debug_racer_profile`: 選手登録番号を指定すると公式の選手プロフィールページ（`racersearch/profile?toban=`）を取得し、「コース別」「進入」等の統計が既に集計されて載っているか調査する。**もし公式側で既にコース別成績が用意されているなら、レースを一から集めて計算し直す必要がなく大幅に楽になる**ため、まずこれを確認する方針
7. 成績記録ページのUIで、決まり手調査ツールが見つけた登録番号をボタンで選手ページ調査欄にセットできるようにした

**次のステップ**:
1. ユーザーの実機で`/debug_racer_profile`を実行し、選手個人ページに公式集計済みのコース別成績があるか確認する（**次の最重要検証ポイント**）
2. 無ければ、選手個人の過去レース結果を収集して逃げ率・逃がし2着率等を自前で計算する仕組みを新規構築する（`/scrape_nige_stats`を選手単位に拡張する形）
3. 精度が十分なら、直前情報（展示タイム・ST等）の自動取得の仕組みを構築
4. 両方を組み合わせた自動予想パイプラインを構築し、しばらくは手動入力と並行稼働させて予想が一致するか確認
5. 定期実行（スケジュール）で日々のレースを自動収集・自動記録できるようにする（結果の自動反映は`/fetch_result`が既にあるので活用）

**方針転換の検討（2026-07-07・ユーザー提案）**: 選手プロフィールページの「コース別成績」タブを調べたところ、**タブの見出しはあるが実データはJSで後から描画されておりurllibの静的HTML取得では拾えないことが判明**（`has_percent_nearby`が false）。この問題（第三者サイト依存の逃げ率／JS描画待ちのコース別成績）を回避する代替案をユーザーが提案:「情報が多すぎると中途半端な予想になる」という仮説（単一要素モデル比較の結果とも一致）を踏まえ、**イン逃げ率に依存しない、公式サイトから簡単に取得できるシンプルな要素（グループB中心）だけで予想する「v-auto」を作り、要素の重み付けも検証で決める**方針に転換。グループB単独は既に335レース検証で本命的中率14%（v9の14.3%とほぼ同等）を記録済みで、イン逃げ率のような取得困難なデータへの依存を避けられる利点がある。
- `/debug_racelist_stats`（2026-07-07実装）: 出走表ページに級別ラベル（A1/A2/B1/B2）・勝率らしき数字が静的HTMLで含まれているか、JS描画の痕跡がないかを調査するツール。成績記録ページの決まり手調査ツールに「出走表を確認」ボタンとして追加。curlでの動作確認済み（サンドボックスはboatrace.jpに到達不可、実機での確認待ち）
- **次の検証ポイント**: 出走表ページのデータが静的HTMLで取得できれば、イン逃げ率問題を完全に迂回して「v-auto」（グループB中心）の実装に進める見込み
- **✅確認済み（2026-07-07）**: `/debug_racelist_stats`で出走表ページを調査したところ、選手勝率（全国/当地）・級別（A1/A2/B1/B2）・モーター成績（2連率/3連率）がすべて静的HTMLに含まれていることを確認（JS描画の痕跡なし）。ユーザーが実際のHTML片を貼ってくれたことで、正確な構造（`is-boatColorN is-fs14`が各艇ブロックの先頭マーカー、`toban=`リンクで選手登録番号、`is-fColor\d">A1</span>`で級別、「数値<br>数値<br>数値」の3つ組が勝率/2連率/3連率の並び）を特定できた
- **✅実装済み（2026-07-07）**: `_parse_racelist_html(html)`を追加。艇ごとにブロック分割し、登録番号・氏名・級別・F数・平均ST・全国勝率/2連率・モーター2連率を正規表現で抽出する。ユーザー提供の実HTML片で単体テスト済み（登録番号3623・深川真二選手・A1級・F0・平均ST0.14・全国勝率7.51・モーター2連率30.16を正確に抽出）。`/debug_racelist_parse`エンドポイントと「🧪 パーサーで6艇分を解析してみる」ボタンで、実際のレースで6艇まとめて解析できるか確認可能
- **✅実機検証完了（2026-07-07・桐生12R）**: 初回は級別（A1以外）が空欄になる不具合があったが、A1は色付きspanタグ、それ以外の級はspan無しプレーンテキストという構造の違いに対応する修正（登録番号直後の「/ 級別」パターンで両対応）で解消。**6艇すべて（A1×3, B1×2, A2×1）で登録番号・氏名・級別・F数・平均ST・全国勝率・全国2連率・モーター2連率が正確に取得できることを確認済み**。これでグループBモデル「v-auto」に必要な公式データの自動取得ルートが完成した
- **次のステップ**: このパーサーの出力（boats配列）を`predict_single_factor(boats, venue, group='B')`にそのまま渡せる形式にし、「会場+レース番号+日付を指定→出走表を自動取得→予想を自動生成」というエンドツーエンドの自動予想パイプラインを構築する。その後、実際のレースで自動予想と現行の手動予想を並行して比較検証する
5. モデルの採否判断は人の確認を残す（自動化しない）

## 未実装・保留中のアイデア

- **v8候補（データ蓄積中）**: 2026-07-04からST考察（安定率/抜出率/出遅率）・ST順位・部品交換・調整重量を`extra_stats`としてinput_dataに保存開始。予想には未使用。有望案: 1号艇の出遅率を勝負レース判定や裏熊の弱点条件に、部品交換をモーター評価の警戒サインに。**100レース貯まったらバックテストで検証**
- **v9候補（2026-07-05データ蓄積開始）**: 「超展開データ」（直接対戦の決まり手 勝ち数/試行数）を`extra_stats.cho_tenkai`として保存開始。1コース=受け方（逃げ/差され/まくられ/捲り差れ/その他/抵抗）、2〜6コース=仕掛け方（差し/まくり/まくり差し/その他/絞り）。各[勝ち,試行]ペア。予想には未使用。狙い: 決まり手率だけでなく試行回数を伴う成功率で信頼度を測る（例: まくり2/2とまくり2/12を区別）、1号艇の「抵抗」回数で粘り強さを評価。サンプル数が少ない項目（試行1〜2回等）は誤差が大きいため、活用時は試行回数の足切りが必須。パーサー`parseChoTenkai()`は実データで単体テスト済み
  - **既存の決まり手系データとの関係（2026-07-05整理）**: 決まり手系データは2種類ある。(1)`kimari_full`＝選手個人の全体的な差し/まくり率（v5で使用→効果なしと確定）。超展開データは**今節の対戦相手固有**の成功率を試行回数つきで持つため、こちらの**上位互換**。(2)`kimari`のsim（`nige_2nd_rates`/`nige_3rd_rates`、v7で使用中）＝会場コース単位の逃げシミュレーション統計。これは対戦相手に依存しない別種の情報なので**超展開データでは代替できず、引き続き必要**
  - **位置づけ（ユーザー確認済み・2026-07-05）**: 「v5をやり直す」のではなく、**現行モデル（v6+v7）はそのまま維持し、その上に超展開データによる補正を追加する**という認識で進める。v5という名前に引きずられず、次の新規バージョン（v9）として現行に足し込む形で設計・検証すること

- **対抗の配当が低い問題（2026-07-05ユーザー指摘）**: v6で勝負レース（65%以上）の対抗も1位艇1着固定にしたため、対抗の配当が本命同様10〜40倍程度に収まりやすくなった（以前の逆転シナリオ20〜60倍は勝負レースでは使われない）。的中率・回収率は改善したので現状は妥当なトレードオフだが、ユーザーは物足りなさを感じている。次の検証候補: 対抗3点のうち1点だけ意図的に逆転シナリオ（2位艇1着）を残すハイブリッド案。**v7が50〜100レース貯まって評価が固まってから検討すること**（今すぐは変更しない、v7の効果測定と混同しないため）
  - **中穴帯を狙いたいというユーザーの関心（2026-07-05）**: 「本命は堅いが2〜3着に紛れがあるレース」＝中穴決着を取りたいという相談があった。本命/大穴（裏熊）と違い中穴は市場評価も割れる最も読みにくい帯だが、上記の「対抗1点だけ逆転シナリオ残し」がまさにその対策になる。将来的には超展開データ（差し/まくりの成功率）で「本命艇は堅いが2〜3コースに紛れを起こす攻め手がいる」レースを条件付きで検出し、その時だけ対抗の配分を中穴狙いに寄せる、という発展形も検討候補
  - **「1着1号艇からの中穴」を取りたい（2026-07-05・重要な指摘）**: ユーザーは「1着は1号艇のままでよく、2着・3着に食い込む中穴（攻め手の艇が実力以上に絡むパターン）を取りたい」と明言。現状の対抗v6は2着・3着を**スコア順**（実力順）で選んでいるため、スコアは低いが差し/まくりが強い「攻め役」の艇が2〜3着に来る中穴を構造的に拾えていない。**次のv10候補**: 対抗3点のうち最低1点を「1号艇1着固定＋差し率/まくり率トップの艇を2着（or 3着）」に置き換える。v7（本命3着を逃がし3着率に差し替え）と同じ発想を対抗の2〜3着に拡張するイメージ。使うデータは既存kimari_fullまたは将来の超展開データ（cho_tenkai）。v7の評価が固まってから着手すること
- 対抗1点目: スコア1位2位が拮抗している時だけ逆転シナリオを入れる条件分岐（提案済み・保留、上の配当問題と統合して再検討）
- ケリー基準の資金管理（賭け金推奨額）— EVキャリブレーション完了後
- 裏熊の得意会場リスト（データ蓄積後）
- 会場別的中率ランキング／対抗の内訳分析
- **勝負条件からイン逃げ率を外し会場のみで判定する案（2026-07-07ユーザー仮説）**: 「会場によってはイン逃げ率が低くても本命対抗が普通に勝つケースがある」という仮説。今日の実戦結果が悪かったことがきっかけだが、1日の結果だけでロジックを変えるのは時期尚早。検証方法: 得意会場ごとに「逃げ率65%以上に絞った回収率」と「逃げ率を問わず会場全体（`nige_min:0, nige_max:100`）の回収率」を比較し、後者が同等以上なら逃げ率条件を外す根拠になる。会場ごとに結果が割れる可能性もある。**まだ未着手、後日検証**
- **単一要素モデルの比較検証（2026-07-07ユーザー仮説・重要）**: 「様々なデータを複合的に考えると中途半端になり、それでうまくいかない可能性があるのでは」という仮説。現行モデルは展示タイム・ST・チルト・級別・モーター・会場コース別・風・展開補正を固定係数で単純に足し引きしているだけで、真の意味での重み最適化されたアンサンブルではないため、理論的に十分あり得る。プロの舟券師は展示タイム・STを最重視し他要素は補足的に使う階層的判断が実態に近いという見立てを共有し、ユーザーも同意。**検証方針（ユーザー確定・2026-07-07）**: まず細かく分けすぎず、大きく2グループで比較する
  - グループA「当日の調子」: 展示タイム・展示ST・チルト・周回/直線/まわり足
  - グループB「地力・機材」: 選手勝率・級別・モーター・会場×コース別1着率
  - 今の複合モデルと合わせて計3パターンをバックテスト（v5/v7のvariant切り替えと同じ発想で、input_data保存済みレースを使って再予想し的中率・回収率を比較）
  - さらに細かい単独検証の候補（グループ比較で方向性が見えてから、必要なら）: 選手勝率単独／周回直線まわり足単独／会場コース別1着率単独／級別単独／超展開データ単独／イン逃げ率（決まり手sim）単独。ただし各パターンに最低30〜50レースは必要なため、細分化しすぎるとデータ不足で判断できなくなる点に注意
  - **✅実装済み（2026-07-07）**: `app.py`に`_score_boat_group(b, venue, group)`と`predict_single_factor(boats, venue, group)`を追加。グループAは展示タイム・展示ST・チルト・F持ち・周回/直線/まわり足のみ、グループBは選手勝率・級別・モーター・会場×コース別1着率のみでスコアを算出し、v6/v7のような複合ロジックは使わず**単純にスコア順で本命2点+対抗3点を選ぶ**（純粋な単一要素比較のため）。本命/対抗/裏熊/中穴の通常ロジックには一切影響しない。成績記録のバックテストカードで「🅰️ グループA単独」「🅱️ グループB単独」がvariant選択肢に追加され、既存のv5/v7比較と同じ画面・同じ絞り込み（期間・会場・逃げ率・本命オッズ下限）で使える。curlでの動作確認済み（3レースのテストデータでグループA/Bそれぞれ異なる予想を出すことを確認）
  - **初回335レース検証結果（2026-07-07）**: 保存時（実戦v6/v7）本命12.5%/対抗14%/回収率88.2%。グループA単独（当日の調子）は本命4.2%/対抗5.4%/回収率53.8%と大幅に弱い。グループB単独（地力・機材）は対抗10.4%/回収率74.3%で複合より劣るが、**本命的中率だけは14%と複合モデル(12.5%)を上回った**。結論: 「複合すると薄まる」仮説は support されず、複合モデルが単独より優れている。ただし本命選定に限ればグループB（選手勝率・級別・モーター・会場コース別）の寄与が大きい可能性があり、**ユーザーは今後「複合モデルの中でグループBの重みを少し強める」方向を検討中**
  - **✅修正済み（2026-07-07）**: 初回検証時、グループAの実装が「展示ST」のみを使い「平均ST」を含めていなかった（複合モデルのベーススコアは平均STを使用）。`_score_boat_group`のグループAに平均ST項を追加（展示STと両方を反映）。この修正後の数字で335レースを再検証すること（修正前の4.2%がどれだけ変わるか要確認）

## 別PCでの再現手順

1. リポジトリをclone/pullする（ブランチ: `claude/busy-albattani-1yg7l4` が開発ブランチ）
2. `python boatrace-web/app.py` で起動（Flask・ポート5001）
3. **history.db はgit管理外**。成績データを移すには旧PCの `boatrace-web/history.db` をコピーすること
4. 新しいClaudeセッションには「CLAUDE.mdを読んでから作業して」と言えばこのファイルを読んで文脈を引き継ぐ
