# 🐻㊙裏熊予想ツール — プロジェクトメモリ

このファイルは競艇（ボートレース）予想ツールの開発コンテキストを新しいセッションに引き継ぐためのもの。
コードを触る前に必ず全部読むこと。

## プロジェクト概要

- **目的**: ユーザーが公式サイトのデータを貼り付けて三連単の買い目予想を出し、成績を記録・分析する個人用Webツール。最終目標は「最強の予想ツール」＝回収率100%超の安定運用
- **構成**: Flask (Python) + SQLite (`boatrace-web/history.db`) + テンプレート2枚（`index.html`=予想画面, `history.html`=成績記録）
- **起動**: ユーザーはWindows。`cd C:\Users\user\test && git pull && python boatrace-web/app.py`（ポート5001）
- **ユーザーへの説明は日本語**。技術用語はかみ砕く。ユーザーは開発者ではないが鋭い指摘をする

## 現在の予想ロジック（MODEL_VERSION: v4, 2026-07-03〜）

`app.py` の `predict()`:

- **スコア計算**: 展示タイム・平均ST・チルト・F持ち・級別（A1+0.8/A2+0.4/B1 0/B2-0.5、展示ST悪いほど級差拡大）・周回/直線/まわり足（当日×0.6+平均×0.4、**小さいほど速い=高評価**。v4でここの符号バグを修正した）・選手勝率・モーター・会場×コース別1着率・風・展開補正（展示STのコース間比較）
- **買い目構成**: 本命2点＋対抗3点（中穴は廃止済み）＋フロント側で万舟2点（オッズ100〜200倍から）
  - 本命2点: スコア1位艇が1着の上位2コンボ
  - 対抗1点目: スコア2位艇が1着（逆転シナリオ）
  - 対抗2点目: 1位艇1着＋展示ST最速艇が2着（展開シナリオ）
  - 対抗3点目: used_combosで重複除外した次点
- **確率化**: candidatesのスコアをsoftmaxで確率化（`prob`）→ EV表示に使用
- **ロジックを変更したら `MODEL_VERSION` を必ず上げる**（v5, v6...）。コメントに日付と変更内容を書く

## 買い方の戦略（ユーザーと合意済み）

- **勝負レースの条件**: イン逃げ率65%以上 ＋ 本命1点目オッズ7倍以上 ＋ 得意会場（その会場×逃げ率帯で回収率80%以上）
- 会場絞り込みの効果は実証済み: 全体回収率67% → 絞ると108%
- 判定はindex.htmlの「勝負レース判定バナー」が自動化済み。会場判定は「今回の逃げ率を5%刻みで切り下げた値〜100%」の帯で過去成績を見る（**下の帯を混ぜてはいけない**、ユーザーの強い意向）
- 万舟2点は検証継続中（65%以上絞りで回収期待値173%だったがサンプル4回。50レース貯めて判断する約束）
- 本命が10倍以下の堅いレースは回収困難。見送りか本命のみ推奨

## 実装済みの主要機能

1. **データ貼り付けパース**: 一括貼り付け／モーター＋選手情報（級別含む）／今節展示情報＋展示情報の統合パース／オッズ
2. **F持ちは手動のみ**（自動判定は廃止済み、ユーザーの意向）
3. **会場・レース変更時に全クリア**（選手の級バッジも消える）
4. **成績記録**: 予想保存（オッズ・入力データ・モデルバージョン込み）→結果入力（公式サイト自動取得あり）→集計
5. **成績分析**: サーバー側集計 `/history_stats`（デバウンス200ms、行はDOM show/hide）。種別別的中率・平均配当・回収期待値、**外れ方内訳**（本命・対抗それぞれ: 的中/3着だけズレ/2・3着ズレ/1着から外れ）、会場別、風向き別、日別、収支推移
6. **成績記録の初期表示は「当日」**（JST）。当日/7日/30日/全期間ボタン
7. **バックテスト** `/backtest`: input_data付きレースを現行ロジックで再予想して保存時予想と比較。**2026-07-03以降の保存分のみ対象**（それ以前はinput_dataなし）
8. **EV表示**: 各買い目に EV=モデル確率×オッズ。100%以上緑。確率は未キャリブレーション（粗い）と明言済み
9. **予想は倍率帯で再分類しない**: サーバーのtype割り当てをそのまま表示（classifyByScore、TYPE_LIMITS制限なし）。以前は倍率帯で上書きしていて対抗の逆転シナリオが表示されないバグがあった

## 重要な履歴・教訓

- **2026-07-03にタイム系スコアの符号バグを修正**（周回/直線/まわり足が「遅いほど高評価」になっていた）。それ以前の成績データは全部バグあり予想の成績。新旧を混ぜて判断しない
- **対抗の新ロジック（3シナリオ）も2026-07-03稼働開始**。旧対抗（全部1位艇1着固定）の成績: 65%絞りで15.1%。新ロジックの評価は50レース貯まってから
- 外れ方内訳の知見（65%絞り・旧データ）: 本命の1着艇は72.7%のレースで正解。最大の改善余地は「3着だけズレ」18.9%と「2・3着ズレ」31.5%＝**2着3着の精度が次の課題**
- ユーザーは「モデル凍結期間」に同意済み: データが貯まるまでロジックをいじらない
- 検証で数字を示すときは的中率の分母に注意: **レースベース**（1レース=1カウント）で統一済み。点数ベースと混ぜない

## 未実装・保留中のアイデア

- 対抗1点目: スコア1位2位が拮抗している時だけ逆転シナリオを入れる条件分岐（提案済み・保留）
- EV確率のキャリブレーション（データが貯まったら実測的中率と突き合わせ）
- ケリー基準の資金管理（賭け金推奨額）
- 本命オッズ帯別の回収率分析（オッズ記録は開始済みなのでデータは貯まっている）
- 会場別的中率ランキング／対抗の内訳分析

## 別PCでの再現手順

1. リポジトリをclone/pullする（ブランチ: `claude/busy-albattani-1yg7l4` が開発ブランチ）
2. `python boatrace-web/app.py` で起動（Flask・ポート5001）
3. **history.db はgit管理外**。成績データを移すには旧PCの `boatrace-web/history.db` をコピーすること
4. 新しいClaudeセッションには「CLAUDE.mdを読んでから作業して」と言えばこのファイルを読んで文脈を引き継ぐ
